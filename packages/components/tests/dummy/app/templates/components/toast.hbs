{{page-title "Toast component"}}

<h2 class="dummy-h2">Toast</h2>

<Hds::Alert
  @type="inline"
  @color="warning"
  @title="TEST ALERT without actions"
  @description="This should not show the `actions` block in the DOM (or show it empty)."
/>

<br />

<Hds::Alert
  @type="inline"
  @color="warning"
  @title="TEST ALERT with actions"
  @description="This should show the `actions` block in the DOM."
>
  <:actions as |A|>
    <A.Button @text="Action" @color="secondary" />
  </:actions>
</Hds::Alert>

<h2 class="dummy-h2">Toast</h2>

<Hds::Toast
  @color="warning"
  @title="TEST TOAST without actions"
  @description="This should not show the `actions` block in the DOM (or show it empty)."
/>

<br />

<Hds::Toast
  @color="warning"
  @title="TEST TOAST with actions"
  @description="This should show the `actions` block in the DOM."
>
  <:actions as |A|>
    <A.Button @text="Action" @color="secondary" />
  </:actions>
</Hds::Toast>