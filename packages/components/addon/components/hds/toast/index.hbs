<Hds::Alert
  class="hds-toast"
  @type="inline"
  @color={{@color}}
  @icon={{@icon}}
  @title={{@title}}
  @description={{@description}}
  @onDismiss={{@onDismiss}}
  ...attributes
>
  <:actions as |A|>
    {{#if (has-block "actions")}}
      <pre>ACTIONS BLOCK IN TOAST</pre>
      {{yield A to="actions"}}
    {{/if}}
  </:actions>
  <:generic as |G|>
    {{#if (has-block "generic")}}
      {{yield G to="generic"}}
    {{/if}}
  </:generic>
</Hds::Alert>